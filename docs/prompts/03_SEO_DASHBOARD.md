# Prompt 3 — Owner-facing SEO and site-health dashboard (updated)

Copy everything below into a new development task and fill in the bracketed inputs.

> Improve the owner-facing dashboard in [REPOSITORY] for [DOMAIN(S)] and assigned scope [FILES]. Inspect the current `src/SeoDashboard.tsx`, styles, API/server diagnostics, tests, data sources, Firebase configuration, and deployment before changing anything. The owner needs a clear dashboard covering up to 107 websites without entering each backend. **Never fabricate live values or label imported/stale/estimated data as current.** Every metric needs source, timestamp, status, and a drill-down. If a source is unavailable, display `Not connected` or `Unknown`, not zero or success.
>
> Reduce clutter with tabs: Overview, Diagnostics/Data Health, Indexing, Protected URLs/Backlinks, Domain Authority, Portfolio, and Workflow. Put **seven large, genuinely useful vital cards** at the top, with owner and Chrizzille confirming the final seven labels: websites healthy/migrated, live 200 pages, broken 404/5xx location URLs, verified Google indexed URLs, valuable URLs preserved/missing, critical data-health failures, and mobile performance (or an owner-approved substitute). Make cards clickable to exact URL lists. The main boxes should prioritize line graphs for changes over time (indexed vs non-indexed, 404/5xx, data failures, migrations, speed); show “insufficient history” instead of a fake trend. Use clear green/yellow/red thresholds with accessible text, not color alone.
>
> Run scheduled diagnostics about every 12 hours with a visible last-run timestamp and on-demand refresh permissions. Check: (1) location URLs returning 404/410/5xx from a crawl or the **actual serving platform** logs; (2) Firestore `cities` with missing/invalid `stateId` or `countyId`, empty essential content, and orphan parent references; (3) top 10 broken 404 URLs; (4) Firebase connectivity and measured Firestore fetch latency. Record failures/timeouts honestly. Firebase Hosting logs only cover Firebase-hosted requests. If the site is served by Vercel, use Vercel-compatible logs or an independent crawl for its traffic; do not mislabel Firebase Hosting logs as Vercel evidence. For web errors, use supported web monitoring/Google Cloud Logging, not a claim that mobile Crashlytics automatically captures all web page errors.
>
> Support optional secure Google Search Console authorization for clicks, impressions, queries, sitemaps and URL Inspection. Display *Google Verified Indexed*, *Google Verified Not Indexed*, *Google Status Unknown*, *Ahrefs Crawled*, and *Ranking Detected — Likely Indexed* separately. `site:` searches are an estimate only. Request Indexing is not an indexed status. Direct exact-URL inspection may be quota-limited; do not manufacture a complete indexed/non-indexed count from partial samples. Show coverage source and freshness. No password collection in the dashboard.
>
> Provide an owner-visible protected URL registry from Ahrefs CSV with historical exact URL/slug, topic, backlinks/referring domains/dofollow/UR/DR/traffic, matching rebuilt URL, live status, self canonical, sitemap/internal-link status, content restoration, owner, GSC verification and checked date. Preserve page intent; location, equipment, and articles belong in matching sections. Prioritize top 25 and record real rebuild/200/canonical/index results individually. Include portfolio health, technical SEO issue URL lists, explainable SEO score, desktop/mobile PageSpeed and CWV, keyword rankings/cannibalization, city-size tracking, content quality, CSV/PDF export, assignment/notes/approval/audit history as staged features—do not falsely mark deferred modules complete.
>
> Ahrefs **DR** and Moz **DA** are different third-party metrics. Pull each only through an authorized API or dated owner-uploaded export, display its source and update time, and never substitute one for the other. A site cannot accurately infer current DA/DR by crawling itself. Show missing integration honestly.
>
> Secure every admin/API endpoint with server-side authorization and least-privilege Firebase access; keep API keys server-side, rate-limit costly diagnostics, avoid exposing full company data publicly, and test Firestore rules. Deliver code, source-to-card map, scheduled job configuration, failure states, representative historical graphs, security tests, and direct runtime verification. Report what is locally implemented, deployed, connected, sampled, and unverified separately. Do not claim 100% accuracy or deployment from a build or HTTP 200 alone. Do not push/deploy without review of shared files and environment.
